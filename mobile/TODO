- associate auth redirect url with website (after prod, see https://reactnavigation.org/docs/deep-linking/#set-up-with-bare-react-native-projects)
- login with google & facebook
- implement useQuery for areas and services
  - add fallback when there are no services or areas
- verify access token before every request
- tester l'app en mode offline
- scrollable screenview
- change language / theme on sign up